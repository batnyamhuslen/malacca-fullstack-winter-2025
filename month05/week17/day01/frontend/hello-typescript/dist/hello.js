"use strict";
// .ts өргөтгөлтэй — TypeScript файл
const message = "Hello, TypeScript!";
console.log(message);
// Type заавал биш — TypeScript дүгнэж (inference) болно
const count = 42; // TypeScript: count is number
const names = ["Alice", "Bob"]; // TypeScript: names is string[]
